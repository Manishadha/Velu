# --- App configuration ---
HOST=0.0.0.0
PORT=8000
TASK_DB=/data/jobs.db

# --- API & security ---
API_KEYS=
CORS_ORIGINS=http://localhost,http://127.0.0.1

# --- Monitoring ---
METRICS_USER=admin
# Plaintext password stored only locally in monitoring/prom_basic_pass.txt

# --- Mode ---
UVICORN_RELOAD=0
DISPATCH_SYNC=0

# auth mode
AUTH_MODE=strict

# choose either API keys OR JWT (or both)
API_KEYS=                    # empty if you’ll use only JWT

# JWT settings (required if using JWT)
JWT_SECRET=<a long random secret>   # e.g. 32+ random bytes base64 or a long hex
JWT_AUDIENCE=velu-api
JWT_ISSUER=velu
JWT_ALG=HS256
