name: lint-and-test

on:
  pull_request:
    branches: [ main, dev ]
  push:
    branches: [ main, dev ]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [ "3.12" ]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"

      - name: Configure git identity
        run: |
          git config --global user.email "ci@velu.local"
          git config --global user.name  "Velu CI"

      - name: Install dependencies
        run: |
          python -m pip install -U pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          if [ -f tests/requirements.txt ]; then pip install -r tests/requirements.txt; fi
          # ensure core frameworks present
          pip install fastapi uvicorn pytest ruff black

      - name: Lint (non-blocking, 2025 syntax)
        run: |
          # Ruff CLI changed: use "check" instead of bare dot
          ruff check .
          # Black check only warns (doesn't break CI)
          black --check . || true


      - name: Run tests
        env:
          API_KEYS: ""
          PYTHONPATH: src
        run: pytest -q
